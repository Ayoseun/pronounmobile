package com.mushroom.trial.utils

import com.mushroom.trial.model.ItemList

interface CellClickListner {
    fun onCellClickListener(curImage: ItemList)
}
interface CellClickListner2 {
    fun onCellClickListener2(eduImage: ItemList)
}
